import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Public vertical slice: fully static-friendly, no external image domains.
  // All imagery is local/SVG by design (see docs/05 — seeded simulation tier).
};

export default nextConfig;
